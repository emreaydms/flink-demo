# AI Usage Disclosure

**Tool used:** Claude (Anthropic)

**What it was used for:**

- Generating the initial slide deck structure and layout code (PowerPoint)
- Generating the boilerplate scaffold for this README

**What was NOT AI-generated:**

- The technical content (Flink concepts, architecture, API usage) was researched from the official Apache Flink documentation and verified manually
- All Python example code (`word_count.py`, `fraud_detection.py`) was reviewed, understood, and tested before inclusion
- The `docker-compose.yml` configuration was verified against the official Flink Docker Hub image documentation
- This disclosure itself

**Note:** All AI-generated output was reviewed and edited before submission. No unreviewed AI output was included in this repository.
